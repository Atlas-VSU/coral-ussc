export enum PaymentStatus {
  PENDING = "pending",
  VERIFIED = "verified",
  REJECTED = "rejected",
}

export enum FineStatus {
    UNPAID = "unpaid",
    PARTIAL = "partially_paid",
    PAID = "paid",
    PENDING = "pending",
    WAIVED = "waived",
    }